
#this is used to scan if functions are present other than main app file
spring.cloud.function.scan.packages=com.tsm.function.controller

#defining default function (called with http://localhost:8080)
spring.cloud.function.definition=getAllStudent

#starting delayed evictData of schema as part of SessionFactory shut-down' -- Error
spring.jpa.show-sql = true
#spring.jpa.hibernate.ddl-auto = update
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect